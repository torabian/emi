% ------------------------------------------------------------------
% Emi Compiler Documentation -- LIGHT palette entry point.
%
% This file only selects the light palette; the actual document lives
% in main-body.tex (shared with dark.tex, the dark-palette entry
% point). Build with `make docs-pdf-light` (or `make docs-pdf` for
% both palettes) from the repository root, or directly: tectonic main.tex
% ------------------------------------------------------------------
\newif\ifemidark
\emidarkfalse
\input{main-body.tex}
