emi: dto
name: singleDto
fields:
  - name: min
    type: int?
    description: Minimum number which can be generated
  - name: max
    type: int
    description: Maximum number which can be generated
  - name: count
    type: int
    description: >-
      How many numbers you want to be generated based on maximum and
      minimum
  - name: nullableObject
    type: object?
    description: 
      This object is optional. Can be passed or not.
      On typ has ? operator
    fields:
      - name: firstName
        type: string

  - name: staticArray
    type: array
    fields:
      - name: item12
        type: string
  - name: nullableArray
    type: array?
    fields:
      - name: item5
        type: string
  - name: staticObject
    type: object
    description: 
      This object is not nullable. On classes, will be always instantiated,
      also on types, is not having ? operator
    fields:
      - name: firstName
        type: string